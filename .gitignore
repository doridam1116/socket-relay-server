# 📦 의존성 모듈 - npm/yarn으로 설치되므로 버전만 관리하고 실제 파일은 무시
node_modules/

# 🔐 환경변수 파일 - API 키나 DB 정보 등 민감한 데이터가 들어있을 수 있음
.env*
# 예: .env, .env.production 등 모든 .env 관련 파일

# 🧾 로그 파일 - 실행 중 생성되는 로그, 불필요하고 용량만 차지함
logs/

# 🛠️ 빌드 결과물 - 소스 코드가 아닌 자동 생성된 결과물이므로 무시
dist/

# 🔐 SSL 인증서 및 키 파일 - 민감한 보안 정보, 절대 유출되면 안 됨
*.pem
*.key
*.crt
